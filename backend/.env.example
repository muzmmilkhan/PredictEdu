# MongoDB Atlas connection string used by the backend service.
# Copy this file to .env (which must NOT be committed) and fill in real values.
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster-host>/?appName=Cluster0
